[build]
  framework = "vite"
  publish = "dist/"
  command = "pnpm run ci && pnpm run test && pnpm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[dev]
  autoLaunch = false
  targetPort = 5173
